all:
  hosts:
    aem-publish:
      ansible_host: $HOST_IP
      ansible_port: 22
      vars:
        sling_run_modes: "publish,local"
        aem_port: '4503'
        aem_mode: 'publish'
        aem_debug: false
